language: node_js
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
dist: trusty
script:
  - npm run test
#  - npm run build -- --prod
#before_install:
#- google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost